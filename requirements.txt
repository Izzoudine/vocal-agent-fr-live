# Core framework
pipecat-ai[websocket,silero]>=0.0.103
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0

# STT - Speech to Text
faster-whisper>=1.1.0

# TTS - Text to Speech (installed from GitHub — PyPI packages are broken)
# MeloTTS: install via git in Dockerfile
# Chatterbox: install via git in Dockerfile

# LLM
ollama>=0.4.0

# Memory
mem0ai>=0.1.0

# Utils
python-dotenv>=1.0.0
slowapi>=0.1.9
numpy>=1.26.0
soundfile>=0.12.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.27.0
