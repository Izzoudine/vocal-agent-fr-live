# ============================================================
# vocal-agent-fr-live — Environment Configuration
# ============================================================
# Copy this file to .env and adjust values for your setup.
# ============================================================

# --- Server ---
HOST=0.0.0.0
PORT=8765

# --- Ollama LLM ---
OLLAMA_HOST=http://ollama:11434
# Recommended models (uncomment one):
OLLAMA_MODEL=mistral:7b-instruct-v0.3-q4_0
# OLLAMA_MODEL=openllm-france/lucie-7b
# OLLAMA_MODEL=ministral:8b-instruct-2410-q4_0

# --- STT (faster-whisper) ---
# Model sizes: tiny, base, small, medium, large-v3
# For CPU 8GB RAM: use "small" or "medium"
# For GPU: use "large-v3" or "bofenghuang/whisper-large-v3-french"
STT_MODEL_SIZE=small
STT_LANGUAGE=fr
STT_DEVICE=cpu
# cpu or cuda
STT_COMPUTE_TYPE=int8
# int8 for CPU, float16 for GPU

# --- TTS ---
# Engine: "melo" or "chatterbox"
TTS_ENGINE=melo
TTS_VOICE_ID=fr_FR-melo-voice1
# MeloTTS voice speed (0.5 - 2.0)
TTS_SPEED=1.0
# Chatterbox emotion exaggeration (0.0 - 1.0, only for chatterbox engine)
TTS_EMOTION_EXAGGERATION=0.5

# --- Memory (mem0) ---
MEM0_ENABLED=true
MEM0_STORAGE_PATH=./data/mem0

# --- Security ---
# Comma-separated allowed origins for CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080
# Optional API key (leave empty to disable auth)
API_KEY=
# Rate limit: requests per minute per IP
RATE_LIMIT=60/minute

# --- GPU (optional) ---
CUDA_VISIBLE_DEVICES=0

# --- Logging ---
LOG_LEVEL=INFO
